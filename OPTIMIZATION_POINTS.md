# 黑马点评项目技术优化点（面试亮点）

> 本文档列出了项目中可以优化的技术点，这些优化点可以作为面试时的技术亮点和项目补充说明。

---

## 📋 目录

- [一、性能优化](#一性能优化)
- [二、架构优化](#二架构优化)
- [三、可靠性优化](#三可靠性优化)
- [四、可维护性优化](#四可维护性优化)
- [五、安全性优化](#五安全性优化)
- [六、监控与运维优化](#六监控与运维优化)
- [七、业务优化](#七业务优化)

---

## 一、性能优化

### 1.1 多级缓存架构

**现状**：目前只使用了 Redis 作为缓存层

**优化方案**：
- **本地缓存（Caffeine）**：在应用内存中缓存热点数据，减少 Redis 网络开销
- **二级缓存（Redis）**：分布式缓存，保证多实例数据一致性
- **三级缓存（数据库）**：持久化存储

**技术亮点**：
- 使用 Caffeine 作为本地缓存，支持过期策略、大小限制
- 实现缓存一致性：本地缓存失效时自动从 Redis 加载
- 解决缓存雪崩：多级缓存逐级降级

**面试话术**：
> "我实现了多级缓存架构，使用 Caffeine + Redis + MySQL 三层缓存。热点数据优先从本地缓存读取，命中率可达 95%以上，响应时间从 50ms 降低到 5ms。同时实现了缓存一致性保证机制，当数据更新时自动失效多级缓存。"

---

### 1.2 数据库查询优化

**现状**：部分查询存在 N+1 问题，缺少批量查询优化

**优化方案**：
- **批量查询优化**：将循环查询改为批量查询（如博客列表查询用户信息）
- **索引优化**：为高频查询字段添加复合索引
- **分页优化**：使用游标分页替代 offset 分页，避免深度分页性能问题
- **读写分离**：主从复制，读操作走从库，写操作走主库

**技术亮点**：
- 使用 MyBatis-Plus 的批量查询方法
- 实现游标分页（Cursor-based Pagination）替代传统分页
- 配置 MyBatis 二级缓存，减少数据库查询

**面试话术**：
> "我优化了数据库查询性能，解决了 N+1 查询问题。比如博客列表查询，原来需要 N+1 次数据库查询，优化后使用批量查询只需要 2 次。同时实现了游标分页，避免了深度分页的性能问题，在百万级数据下查询时间从 2 秒降低到 200ms。"

---

### 1.3 Redis 批量操作优化

**现状**：部分场景存在多次 Redis 调用

**优化方案**：
- **Pipeline 批量操作**：将多个 Redis 命令打包执行，减少网络往返
- **批量获取**：使用 `MGET`、`HMGET` 等批量命令
- **Lua 脚本优化**：将多个操作合并到 Lua 脚本中，保证原子性

**技术亮点**：
- 使用 Redis Pipeline 批量执行命令，性能提升 5-10 倍
- 在点赞列表、Feed 流等场景使用批量操作

**面试话术**：
> "我使用 Redis Pipeline 优化了批量操作，将多个 Redis 命令打包执行，减少了网络往返次数。在 Feed 流查询场景中，性能提升了 8 倍，从原来的 100ms 降低到 12ms。"

---

### 1.4 异步处理优化

**现状**：部分耗时操作同步执行，影响响应时间

**优化方案**：
- **异步任务处理**：使用 `@Async` 注解处理非关键路径操作
- **线程池优化**：使用 `ThreadPoolTaskExecutor` 替代 `Executors`，支持参数配置和监控
- **消息队列解耦**：将耗时操作放入消息队列异步处理

**技术亮点**：
- 配置自定义线程池，支持核心线程数、最大线程数、队列大小等参数
- 实现线程池监控，实时查看线程池状态
- 使用 Spring 的 `@Async` 实现异步处理

**面试话术**：
> "我将耗时操作改为异步处理，比如博客发布后的 Feed 流推送、缓存预热等。使用自定义线程池替代 Executors，支持参数配置和监控。响应时间从 500ms 降低到 50ms，用户体验显著提升。"

---

### 1.5 Feed 流推拉结合优化

**现状**：目前只实现了推模式，大 V 用户发布博客时推送压力大

**优化方案**：
- **推拉结合模式**：
  - 普通用户：推模式（实时推送）
  - 大 V 用户（粉丝 > 1000）：拉模式（延迟拉取）
- **智能切换**：根据用户粉丝数动态选择推拉模式
- **延迟推送**：大 V 用户使用延迟队列，在低峰期推送

**技术亮点**：
- 实现推拉结合算法，根据用户类型智能选择
- 使用延迟队列处理大 V 用户的 Feed 流推送
- 减少 Redis 存储压力，提升系统性能

**面试话术**：
> "我实现了 Feed 流的推拉结合模式。普通用户使用推模式保证实时性，大 V 用户使用拉模式减少推送压力。通过智能切换，Redis 存储量减少了 60%，推送性能提升了 3 倍。"

---

## 二、架构优化

### 2.1 消息队列升级

**现状**：使用 Redis Stream 作为消息队列，缺少死信队列和消息清理机制

**优化方案**：
- **引入 RocketMQ/RabbitMQ**：专业的消息队列，支持死信队列、消息持久化、消息重试
- **死信队列机制**：处理失败的消息，避免无限重试
- **消息清理策略**：定期清理已处理的消息，避免 Stream 无限增长
- **消息幂等性**：保证消息重复消费不会产生副作用

**技术亮点**：
- 使用 RocketMQ 的延迟消息、顺序消息等特性
- 实现消息幂等性校验，使用 Redis Set 记录已处理消息 ID
- 实现死信队列，失败消息自动进入死信队列，便于人工处理

**面试话术**：
> "我将 Redis Stream 升级为 RocketMQ，实现了更完善的消息队列机制。包括死信队列处理失败消息、消息幂等性保证、消息持久化等。同时实现了消息清理策略，定期清理已处理的消息，避免存储无限增长。"

---

### 2.2 分布式事务优化

**现状**：秒杀场景使用本地事务，跨服务调用缺少分布式事务

**优化方案**：
- **Seata 分布式事务**：解决跨服务事务一致性问题
- **最终一致性方案**：使用消息队列 + 补偿机制实现最终一致性
- **TCC 模式**：Try-Confirm-Cancel 模式，适合高并发场景

**技术亮点**：
- 在秒杀、订单等场景使用 Seata AT 模式
- 实现补偿机制，保证最终一致性
- 使用消息队列实现异步事务

**面试话术**：
> "我引入了 Seata 分布式事务框架，解决了跨服务调用的事务一致性问题。在秒杀场景中，使用 AT 模式保证库存扣减和订单创建的原子性。同时实现了补偿机制，保证最终一致性。"

---

### 2.3 服务拆分与微服务化

**现状**：单体应用，所有功能耦合在一起

**优化方案**：
- **服务拆分**：按业务域拆分为用户服务、商品服务、订单服务、支付服务等
- **服务注册与发现**：使用 Nacos/Eureka 实现服务注册与发现
- **API 网关**：使用 Spring Cloud Gateway 统一入口，实现路由、限流、鉴权
- **配置中心**：使用 Nacos Config 统一管理配置

**技术亮点**：
- 实现微服务架构，服务间通过 Feign 调用
- 使用 Spring Cloud Gateway 实现统一网关
- 实现服务熔断降级，使用 Sentinel 或 Hystrix

**面试话术**：
> "我将单体应用拆分为微服务架构，按业务域拆分为用户、商品、订单、支付等服务。使用 Nacos 作为注册中心和配置中心，Spring Cloud Gateway 作为 API 网关。实现了服务熔断降级，提升了系统的可用性。"

---

### 2.4 缓存更新策略优化

**现状**：使用延迟双删策略，可能存在数据不一致

**优化方案**：
- **Canal 数据同步**：监听 MySQL binlog，自动更新缓存
- **消息队列通知**：数据更新后发送消息，消费者更新缓存
- **版本号机制**：使用版本号判断缓存是否过期

**技术亮点**：
- 使用 Canal 监听数据库变更，自动同步到 Redis
- 实现缓存版本号机制，解决缓存一致性问题
- 使用消息队列解耦缓存更新逻辑

**面试话术**：
> "我使用 Canal 监听 MySQL binlog，实现了缓存自动更新。当数据库数据变更时，自动同步到 Redis，解决了缓存一致性问题。同时实现了版本号机制，保证缓存数据的准确性。"

---

## 三、可靠性优化

### 3.1 限流降级机制

**现状**：缺少限流和降级机制，高并发时可能压垮系统

**优化方案**：
- **Sentinel 限流**：实现 QPS 限流、线程数限流、热点参数限流
- **降级策略**：服务异常时返回默认值或缓存数据
- **熔断机制**：服务异常率达到阈值时自动熔断

**技术亮点**：
- 使用 Sentinel 实现多种限流策略
- 实现降级规则，服务异常时返回友好提示
- 配置熔断规则，保护下游服务

**面试话术**：
> "我使用 Sentinel 实现了限流降级机制。在秒杀接口实现了 QPS 限流，防止系统被压垮。同时实现了降级策略，当服务异常时返回缓存数据或默认值，保证用户体验。配置了熔断规则，当异常率达到阈值时自动熔断，保护下游服务。"

---

### 3.2 消息队列可靠性保障

**现状**：Redis Stream 缺少死信队列，失败消息可能丢失

**优化方案**：
- **死信队列**：处理失败的消息，支持人工介入
- **消息重试机制**：失败消息自动重试，支持指数退避
- **消息持久化**：使用专业的消息队列保证消息不丢失
- **消费确认机制**：只有处理成功才确认消息

**技术亮点**：
- 实现死信队列，失败消息进入死信队列
- 实现消息重试机制，支持最大重试次数和指数退避
- 使用消息队列的持久化机制保证消息不丢失

**面试话术**：
> "我实现了消息队列的可靠性保障机制。包括死信队列处理失败消息、消息重试机制支持指数退避、消息持久化保证不丢失。在秒杀场景中，订单处理失败的消息会自动进入死信队列，支持人工介入处理。"

---

### 3.3 分布式锁优化

**现状**：使用简单的 Redis 锁，可能存在死锁问题

**优化方案**：
- **锁续期机制**：使用 Redisson 的 WatchDog 自动续期
- **可重入锁**：支持同一线程重复获取锁
- **公平锁**：使用公平锁避免线程饥饿
- **锁超时处理**：锁超时自动释放，避免死锁

**技术亮点**：
- 使用 Redisson 的分布式锁，支持自动续期
- 实现可重入锁，避免死锁
- 配置锁超时时间，自动释放

**面试话术**：
> "我优化了分布式锁的实现，使用 Redisson 的 WatchDog 机制自动续期，避免了业务执行时间过长导致锁提前释放的问题。同时实现了可重入锁，支持同一线程重复获取锁，提升了灵活性。"

---

### 3.4 数据库连接池优化

**现状**：使用默认连接池配置，可能在高并发时出现问题

**优化方案**：
- **HikariCP 连接池**：使用高性能连接池
- **连接池参数调优**：根据业务场景调整连接池大小
- **连接泄漏检测**：检测并处理连接泄漏问题

**技术亮点**：
- 使用 HikariCP 替代默认连接池，性能提升 30%
- 根据业务场景调整连接池参数
- 实现连接泄漏检测和告警

**面试话术**：
> "我使用 HikariCP 替代了默认连接池，性能提升了 30%。根据业务场景调整了连接池参数，包括最大连接数、最小空闲连接数等。同时实现了连接泄漏检测，及时发现和处理连接泄漏问题。"

---

## 四、可维护性优化

### 4.1 线程池统一管理

**现状**：使用 `Executors` 静态方法创建线程池，参数不可配置

**优化方案**：
- **线程池配置类**：统一管理所有线程池配置
- **参数可配置化**：通过配置文件配置线程池参数
- **线程池监控**：实现线程池监控，实时查看状态
- **优雅关闭**：应用关闭时优雅关闭线程池

**技术亮点**：
- 创建线程池配置类，统一管理
- 使用 `@ConfigurationProperties` 实现参数配置化
- 实现线程池监控接口，查看线程池状态
- 实现优雅关闭，等待任务完成后再关闭

**面试话术**：
> "我实现了线程池的统一管理，将所有线程池配置集中管理，支持通过配置文件动态调整参数。同时实现了线程池监控，可以实时查看线程池状态。在应用关闭时实现了优雅关闭，等待任务完成后再关闭线程池。"

---

### 4.2 日志系统优化

**现状**：使用简单的日志，缺少结构化日志和链路追踪

**优化方案**：
- **结构化日志**：使用 JSON 格式输出日志，便于分析
- **链路追踪**：使用 SkyWalking/Zipkin 实现分布式链路追踪
- **日志聚合**：使用 ELK（Elasticsearch + Logstash + Kibana）聚合日志
- **日志级别动态调整**：支持动态调整日志级别

**技术亮点**：
- 使用 Logback 输出 JSON 格式日志
- 集成 SkyWalking 实现链路追踪
- 使用 ELK 聚合和分析日志
- 实现日志级别动态调整

**面试话术**：
> "我优化了日志系统，使用 JSON 格式输出结构化日志，便于日志分析。集成了 SkyWalking 实现分布式链路追踪，可以追踪一次请求的完整调用链。使用 ELK 聚合日志，支持日志检索和分析。"

---

### 4.3 代码规范与质量

**现状**：代码缺少统一规范，可能存在代码质量问题

**优化方案**：
- **代码规范**：使用 Alibaba Java Coding Guidelines 规范代码
- **代码检查**：使用 SonarQube 进行代码质量检查
- **单元测试**：提高单元测试覆盖率
- **API 文档**：使用 Swagger/Knife4j 生成 API 文档

**技术亮点**：
- 配置代码规范检查插件
- 使用 SonarQube 进行代码质量分析
- 编写单元测试，覆盖率 > 80%
- 使用 Knife4j 生成 API 文档

**面试话术**：
> "我建立了代码规范和质量保障体系。使用 Alibaba Java Coding Guidelines 规范代码，SonarQube 进行代码质量检查。编写了完善的单元测试，覆盖率达到了 85%。使用 Knife4j 生成 API 文档，提升了开发效率。"

---

### 4.4 配置管理优化

**现状**：配置硬编码在代码中，不便于管理

**优化方案**：
- **配置中心**：使用 Nacos Config 统一管理配置
- **配置热更新**：支持配置热更新，无需重启应用
- **多环境配置**：支持 dev、test、prod 多环境配置
- **敏感信息加密**：敏感配置信息加密存储

**技术亮点**：
- 使用 Nacos Config 作为配置中心
- 实现配置热更新，修改配置后自动生效
- 支持多环境配置，通过 profile 切换
- 使用 Jasypt 加密敏感信息

**面试话术**：
> "我使用 Nacos Config 作为配置中心，统一管理所有配置。支持配置热更新，修改配置后无需重启应用即可生效。实现了多环境配置，支持 dev、test、prod 环境切换。使用 Jasypt 加密敏感信息，提升了安全性。"

---

## 五、安全性优化

### 5.1 接口安全加固

**现状**：缺少接口安全防护机制

**优化方案**：
- **接口签名**：使用签名机制防止接口被篡改
- **参数加密**：敏感参数加密传输
- **防重放攻击**：使用时间戳 + nonce 防止重放攻击
- **IP 白名单**：关键接口配置 IP 白名单

**技术亮点**：
- 实现接口签名验证，使用 HMAC-SHA256 算法
- 使用 AES 加密敏感参数
- 实现防重放攻击机制
- 配置 IP 白名单拦截器

**面试话术**：
> "我实现了接口安全加固机制。使用 HMAC-SHA256 实现接口签名，防止接口被篡改。使用 AES 加密敏感参数，保证数据传输安全。实现了防重放攻击机制，使用时间戳和 nonce 防止请求重放。关键接口配置了 IP 白名单，进一步提升安全性。"

---

### 5.2 数据脱敏

**现状**：用户敏感信息可能直接返回给前端

**优化方案**：
- **数据脱敏**：手机号、身份证等敏感信息脱敏显示
- **权限控制**：不同角色看到不同级别的数据
- **日志脱敏**：日志中的敏感信息自动脱敏

**技术亮点**：
- 实现数据脱敏工具类，支持手机号、身份证、邮箱等脱敏
- 使用 AOP 实现自动脱敏
- 日志中的敏感信息自动脱敏

**面试话术**：
> "我实现了数据脱敏机制，对手机号、身份证等敏感信息进行脱敏处理。使用 AOP 实现自动脱敏，无需在每个接口手动处理。同时实现了日志脱敏，避免敏感信息泄露到日志中。"

---

### 5.3 防刷机制

**现状**：缺少防刷机制，可能被恶意刷接口

**优化方案**：
- **验证码**：关键操作需要验证码
- **频率限制**：同一 IP/用户限制请求频率
- **设备指纹**：识别设备，防止多账号刷接口
- **行为分析**：分析用户行为，识别异常操作

**技术亮点**：
- 使用 Redis 实现频率限制
- 实现设备指纹识别
- 使用机器学习分析用户行为

**面试话术**：
> "我实现了防刷机制，包括验证码验证、频率限制、设备指纹识别等。使用 Redis 实现频率限制，同一 IP 在指定时间内只能请求指定次数。实现了设备指纹识别，防止多账号刷接口。"

---

## 六、监控与运维优化

### 6.1 应用监控

**现状**：缺少应用监控，问题难以定位

**优化方案**：
- **Prometheus + Grafana**：监控应用指标，可视化展示
- **JVM 监控**：监控堆内存、GC 等 JVM 指标
- **业务监控**：监控关键业务指标，如订单量、用户数等
- **告警机制**：指标异常时自动告警

**技术亮点**：
- 集成 Micrometer 暴露指标给 Prometheus
- 使用 Grafana 可视化展示监控数据
- 配置告警规则，异常时自动告警
- 监控关键业务指标

**面试话术**：
> "我使用 Prometheus + Grafana 实现了应用监控。监控了 JVM 指标、业务指标等，通过 Grafana 可视化展示。配置了告警规则，当指标异常时自动发送告警通知，帮助快速定位问题。"

---

### 6.2 性能分析

**现状**：缺少性能分析工具，性能问题难以定位

**优化方案**：
- **APM 工具**：使用 SkyWalking/Arthas 进行性能分析
- **慢查询分析**：分析慢 SQL，优化数据库查询
- **接口性能分析**：分析接口响应时间，找出性能瓶颈
- **压测**：使用 JMeter 进行压力测试

**技术亮点**：
- 使用 SkyWalking 进行性能分析
- 使用 Arthas 在线诊断问题
- 分析慢 SQL，优化数据库查询
- 使用 JMeter 进行压力测试

**面试话术**：
> "我使用 SkyWalking 和 Arthas 进行性能分析。通过 SkyWalking 可以查看接口的响应时间、调用链等，快速定位性能瓶颈。使用 Arthas 可以在线诊断问题，无需重启应用。通过慢查询分析，优化了数据库查询性能。"

---

### 6.3 健康检查

**现状**：缺少健康检查机制

**优化方案**：
- **Actuator 健康检查**：使用 Spring Boot Actuator 提供健康检查接口
- **自定义健康检查**：检查 Redis、MySQL 等外部依赖的健康状态
- **就绪探针**：K8s 就绪探针，判断服务是否就绪
- **存活探针**：K8s 存活探针，判断服务是否存活

**技术亮点**：
- 使用 Spring Boot Actuator 提供健康检查
- 自定义健康检查器，检查外部依赖
- 配置 K8s 探针，支持自动重启

**面试话术**：
> "我使用 Spring Boot Actuator 实现了健康检查机制。提供了健康检查接口，可以检查应用和外部依赖的健康状态。配置了 K8s 探针，当服务异常时自动重启，提升了系统的可用性。"

---

### 6.4 容器化部署

**现状**：传统部署方式，部署效率低

**优化方案**：
- **Docker 容器化**：将应用打包成 Docker 镜像
- **K8s 编排**：使用 K8s 编排容器，实现自动扩缩容
- **CI/CD**：使用 Jenkins/GitLab CI 实现持续集成和部署
- **滚动更新**：支持滚动更新，零停机部署

**技术亮点**：
- 编写 Dockerfile，构建应用镜像
- 使用 K8s 部署应用，支持自动扩缩容
- 配置 CI/CD 流水线，自动化部署
- 实现滚动更新，零停机部署

**面试话术**：
> "我实现了容器化部署，使用 Docker 打包应用，K8s 编排容器。配置了 CI/CD 流水线，代码提交后自动构建和部署。实现了滚动更新，支持零停机部署。使用 K8s 的 HPA 实现自动扩缩容，根据 CPU 和内存使用率自动调整实例数。"

---

## 七、业务优化

### 7.1 秒杀系统优化

**现状**：秒杀系统基本功能已实现，但可以进一步优化

**优化方案**：
- **库存预热**：提前将库存加载到 Redis，减少数据库压力
- **令牌桶限流**：使用令牌桶算法限流，更平滑
- **异步下单**：下单成功后异步处理后续逻辑
- **库存回滚**：订单取消时自动回滚库存

**技术亮点**：
- 实现库存预热机制，秒杀开始前加载库存
- 使用令牌桶算法限流，比固定窗口更平滑
- 实现异步下单，提升响应速度
- 实现库存回滚机制

**面试话术**：
> "我优化了秒杀系统，实现了库存预热机制，秒杀开始前将库存加载到 Redis。使用令牌桶算法限流，比固定窗口限流更平滑。实现了异步下单，下单成功后异步处理后续逻辑，响应时间从 200ms 降低到 50ms。"

---

### 7.2 搜索功能优化

**现状**：缺少搜索功能

**优化方案**：
- **Elasticsearch 全文搜索**：使用 ES 实现全文搜索
- **搜索建议**：实现搜索建议功能
- **搜索结果排序**：支持按相关度、时间、热度等排序
- **搜索高亮**：搜索结果关键词高亮

**技术亮点**：
- 使用 Elasticsearch 实现全文搜索
- 实现搜索建议，提升用户体验
- 支持多种排序方式
- 实现搜索关键词高亮

**面试话术**：
> "我使用 Elasticsearch 实现了全文搜索功能。支持店铺、博客等内容的全文搜索，响应时间在 100ms 以内。实现了搜索建议功能，用户输入关键词时自动提示。支持按相关度、时间、热度等多种方式排序，搜索结果关键词高亮显示。"

---

### 7.3 推荐系统

**现状**：缺少个性化推荐

**优化方案**：
- **协同过滤**：基于用户行为的协同过滤推荐
- **内容推荐**：基于内容的推荐算法
- **热门推荐**：基于热度的推荐
- **实时推荐**：实时计算推荐结果

**技术亮点**：
- 实现协同过滤推荐算法
- 使用 Redis 存储用户行为数据
- 实现实时推荐，用户行为实时影响推荐结果

**面试话术**：
> "我实现了推荐系统，使用协同过滤算法为用户推荐内容。基于用户的浏览、点赞、关注等行为计算相似度，推荐相似用户喜欢的内容。使用 Redis 存储用户行为数据，实现实时推荐，用户行为实时影响推荐结果。"

---

### 7.4 数据分析

**现状**：缺少数据分析功能

**优化方案**：
- **数据统计**：统计用户数、订单数、销售额等
- **数据可视化**：使用 ECharts 可视化展示数据
- **用户画像**：分析用户行为，构建用户画像
- **数据报表**：生成数据报表，支持导出

**技术亮点**：
- 实现数据统计功能，实时统计关键指标
- 使用 ECharts 可视化展示数据
- 构建用户画像，分析用户行为
- 生成数据报表，支持 Excel 导出

**面试话术**：
> "我实现了数据分析功能，实时统计用户数、订单数、销售额等关键指标。使用 ECharts 可视化展示数据，支持多种图表类型。构建了用户画像，分析用户行为特征，为运营决策提供数据支持。"

---

## 📊 优化优先级建议

### 高优先级（立即实施）
1. ✅ 多级缓存架构
2. ✅ 限流降级机制
3. ✅ 消息队列死信队列
4. ✅ 线程池统一管理
5. ✅ 应用监控

### 中优先级（近期实施）
1. ⚠️ 数据库查询优化
2. ⚠️ 分布式事务优化
3. ⚠️ 日志系统优化
4. ⚠️ 接口安全加固
5. ⚠️ 容器化部署

### 低优先级（长期规划）
1. 📋 服务拆分与微服务化
2. 📋 推荐系统
3. 📋 数据分析
4. 📋 搜索功能

---

## 💡 面试建议

### 如何介绍优化点

1. **先说现状**：描述当前存在的问题
2. **再说方案**：提出具体的优化方案
3. **最后说效果**：量化优化效果（性能提升、响应时间降低等）

### 量化指标示例

- 响应时间：从 200ms 降低到 50ms（提升 75%）
- 吞吐量：从 1000 QPS 提升到 5000 QPS（提升 5 倍）
- 缓存命中率：从 60% 提升到 95%
- 数据库查询：从 N+1 次降低到 2 次

### 技术深度展示

- 不仅说出用了什么技术，还要说出**为什么用**、**怎么用**、**效果如何**
- 准备一些**技术细节**，比如缓存一致性如何保证、分布式锁如何避免死锁等
- 准备一些**踩坑经验**，比如遇到过什么问题、如何解决的

---

## 📚 相关技术栈

### 缓存
- Caffeine（本地缓存）
- Redis（分布式缓存）

### 消息队列
- RocketMQ
- RabbitMQ
- Redis Stream

### 监控
- Prometheus + Grafana
- SkyWalking
- ELK

### 微服务
- Spring Cloud
- Nacos
- Sentinel

### 数据库
- MySQL（主从复制）
- Elasticsearch（搜索）

### 容器化
- Docker
- Kubernetes

---

## 🎯 总结

以上优化点涵盖了**性能、架构、可靠性、可维护性、安全性、监控、业务**等多个维度，可以根据项目实际情况和面试需求选择合适的优化点进行深入学习和实践。

**建议**：
1. 选择 3-5 个优化点深入学习和实践
2. 准备详细的技术方案和实现细节
3. 准备量化指标和优化效果
4. 准备可能遇到的问题和解决方案

---

**文档版本**：v1.0  
**最后更新**：2025-01-XX

