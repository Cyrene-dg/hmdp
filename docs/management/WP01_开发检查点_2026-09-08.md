# WP-01 开发检查点（2026-09-08）

> 状态：`DONE`；2026-09-09 完成隔离上下文与最终门禁复验。

## 已完成

- 应用扫描范围同时覆盖 `com.hmdp` 与 `com.qinghe.marketing`；
- 建立 identity、store、campaign、claim、entitlement、redemption、reconciliation、settlement、operations 模块边界；
- 建立业务时钟、业务编号、整数分金额、统一错误类别、traceId、审计事实和 Qinghe Mapper 标记扫描；
- 建立只新增 `qh_` 表、不修改旧 `tb_` 表的 V001 增量迁移，共 18 张核心表；
- 建立仅供无业务数据的临时环境使用的 R001 回滚脚本，并明确真实业务数据产生后只能前向修复；
- 建立架构依赖门禁，禁止 Qinghe 新模块依赖旧 controller/service/entity/mapper；
- 从本机 Redis 服务配置安全读取认证信息用于测试，没有把密码写入源码、文档或命令输出。

## 已执行验证

1. Qinghe 公共组件、架构边界、迁移契约及真实 MySQL 临时库迁移/回滚：`10 PASS`。
2. V001 在随机命名的 `qh_wp01_*` 临时库创建 18 张表，R001 回滚后表数为 0，临时库最终删除：`PASS`。
3. 旧应用 Spring 上下文在注入本机 Redis 认证后启动，并连接 Redis、MySQL、RabbitMQ：`PASS WITH CAVEAT`。

## 发现但未掩盖的问题

- 第一次真实迁移发现 `utf8mb44` 拼写错误，修复后重跑通过；
- `BusinessIdGenerator` 有两个构造器时 Spring 2.3 无法选择，增加显式注入构造器后通过；
- 旧应用启动测试会启动黑马点评 RabbitMQ 消费者并处理遗留队列，产生大量旧 MyBatis-Plus 与当前 JDK 的反射兼容错误；Spring 测试本身最终通过，但该测试有外部副作用，不能作为 Qinghe 的长期 smoke test；
- 本机 Redis 需要认证，而仓库默认 `application.yaml` 未配置密码。认证值不得提交，后续通过环境变量/测试配置注入。

## 2026-09-09 Gate 收口

1. 新增 `QingheIsolatedContextTest`，只扫描 `com.qinghe.marketing`，确认业务时钟、编号器和 TraceId 过滤器可启动，并且没有旧秒杀服务、RabbitMQ 配置或旧启动器 Bean：`PASS`；
2. WP-00、Plus 遗留事务测试和 WP-01 非数据库门禁合并执行：`34 RUN / 29 PASS / 5 EXPECTED SKIPPED / 0 FAILED`；
3. `QingheSchemaMigrationIT` 使用环境变量注入本机认证，在随机临时库执行 V001 创建 18 张表、R001 回滚为 0：`1 PASS`；
4. 额外查询 `INFORMATION_SCHEMA`，活动的 `qh_wp01_*` 临时库数量为 `0`；
5. 移除集成测试源码中的默认数据库密码，认证值不再进入待提交代码；
6. WP-01 与 M1 达到 `DONE`，下一任务为 WP-02 身份与门店适配。

## 仍未声明完成

- WP-02 及后续业务功能尚未开始；
- 当前成果不能表述为领券、发券、POS 核销或对账已经实现。
