# WP-03 开发检查点（2026-09-09）

> 状态：`DONE`。下一工作包：`WP-04 领取受理与 Outbox`。

## 已实现

- 权益模板创建、编辑、查询与列表，以及免费商品券、代金券和相对/固定有效期规则校验；
- 活动草稿、修改、提交、审核、定时生效、到期结束和人工终止状态迁移；
- 审核人与创建人分离，驳回原因必填，驳回后允许原负责人修改并回到草稿；
- 审核通过时在同一事务内写入独立审核事实、不可变发布快照并进入 `SCHEDULED`；
- 发布时重新校验门店启用状态与归属类型，直营店补贴固定为 0，加盟店使用活动统一单张补贴；
- 发布后模板与活动核心规则不可覆盖修改，重复发布返回首次结果；
- `SCHEDULED/ACTIVE` 活动可通过独立调整单增加库存，禁止减库存，一张调整单最多生效一次；
- MySQL 保存权威总库存，Redis 保存可用库存加速值；Redis 初始化及库存增加均通过 Lua 幂等执行；
- 提供 MySQL/Redis 库存一致性检查入口，不在活动单行维护领取热计数；
- V003 前向迁移与 R003 回滚脚本，保留 V001/V002 不变。

## Gate 证据

1. WP-00—WP-03 与遗留事务回归：`89 RUN / 84 PASS / 5 EXPECTED SKIPPED / 0 FAILED`。5 项跳过项仍是 WP-06 才能执行的 POS 业务行为骨架；
2. `QingheWp03PersistenceIT` 在随机 `qh_wp03_*` 临时库验证活动发布、门店补贴快照、模板锁定、重复审核、库存调整一次生效及完整回滚：`1 PASS`；
3. 前向迁移后共有 25 张 `qh_` 表，R003 后为 23 张，R002 后为 18 张，R001 后为 0 张；
4. 集成测试最终删除临时库，额外查询 `INFORMATION_SCHEMA` 得到活动 `qh_wp03_*` 数据库数量为 `0`；
5. `QingheWp03RedisIT` 连接本机 Redis 验证同值初始化幂等、不同值初始化冲突、同一调整单重复执行只增加一次：`1 PASS`，测试使用的两个随机键均已精确删除；
6. `git diff --check` 通过。

核心回归命令：

```text
mvn -q "-Dtest=QingheArchitectureBoundaryTest,QingheIsolatedContextTest,MemberCenterMockServerTest,OpenApiContractStructureTest,PosBusinessContractSkeletonTest,PosContractClientTest,ReconciliationContractFixtureTest,AuthenticationBoundaryTest,HttpMemberCenterClientTest,MemberSessionServiceTest,PosRequestAuthenticatorTest,RedisPlatformSessionCacheTest,QingheSchemaMigrationContractTest,BusinessIdGeneratorTest,MoneyFenTest,TraceIdFilterTest,TraceIdTest,StoreEligibilityAndCredentialServiceTest,StoreImportServiceTest,QingheApiControllerTest,VoucherOrderTransactionServiceTest,AdminCampaignControllerTest,CampaignServiceTest,CampaignApprovalTransactionServiceTest,BenefitTemplateServiceTest,InventoryAdjustmentServiceTest,RedisCampaignStockCacheTest" test
```

真实 MySQL 与 Redis 集成测试通过环境变量注入本机连接信息执行，不在命令、源码或文档中记录凭据。

## 已确认但未冒充完成

- 管理端 Bearer Token 仍是本地/集成环境适配器，不是企业统一身份平台；
- MySQL、Redis 验证只代表本机真实依赖探针通过，不代表生产部署、生产流量或真实厂商联调；
- 当前库存是活动发布和追加调整基线，尚未实现会员领取时的原子预留、申请事实与补偿；
- `ClaimRequest`、Outbox、MQ 发券、权益卡包仍属于 WP-04/WP-05；
- POS 验券、核销和加盟补贴候选仍属于 WP-06；
- 没有真实甲方、真实门店、生产发布或性能数字。

## 下一安全动作

1. 以发布快照和 Redis 初始可用库存作为 WP-04 的稳定输入；
2. 先冻结领取请求、预留和 Outbox 状态机，再实现 Lua 原子预留；
3. 在本地事务内区分 `ClaimRequest(PROCESSING)` 与 `OutboxEvent(NEW)`，不把 MQ ACK 当作发券成功；
4. WP-04 Gate 通过前不开始依赖申请事实的 WP-05。
