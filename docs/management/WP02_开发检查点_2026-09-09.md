# WP-02 开发检查点（2026-09-09）

> 状态：`DONE`。下一工作包：`WP-03 活动与库存基线`。

## 已实现

- 会员中心 Token introspection HTTP 适配器，区分无效、过期、冻结、注销、限流、超时和异常响应；
- 外部会员号到平台会员 ID 的稳定映射，以及随机短期平台会话；仅保存 Token SHA-256，不保存外部或平台 Token 明文；
- MySQL 作为会话权威数据源，Redis 仅作带过期时间的读取加速；Redis 故障时回退 MySQL；
- 版本化门店 CSV 预览和提交，支持 BOM、引号、逐行错误、同版本同摘要幂等及同版本不同摘要冲突；
- 门店启停与直营/加盟归属模型；
- POS ClientId 与密钥引用持久化、同门店凭据轮换和旧凭据撤销，密钥明文不入库；
- 管理端权限适配器、会员 Bearer 会话和 POS HMAC-SHA256 三条独立认证边界；
- POS 时间窗、签名、凭据绑定、停用门店与 nonce 防重放校验；签名通过后才占用 nonce；
- V002 增加 POS nonce 表，R002 支持回退到 WP-01 的 18 张表；
- 平台 OpenAPI 补充会员中心限流对应的 429 契约。

## Gate 证据

1. WP-00、WP-01、WP-02 与遗留事务回归：`68 RUN / 63 PASS / 5 EXPECTED SKIPPED / 0 FAILED`。5 项跳过项均是 WP-06 才能执行的 POS 业务行为骨架；
2. `QingheWp02PersistenceIT` 在随机 `qh_wp02_*` 临时库验证会员映射、会话、门店导入、密钥引用与轮换、nonce 唯一性：`1 PASS`；
3. 前向迁移后共有 23 张 `qh_` 表，R002 后为 18 张，R001 后为 0 张；
4. 集成测试最终删除临时库，额外查询 `INFORMATION_SCHEMA` 得到活动 `qh_wp02_*` 数据库数量为 `0`；
5. `git diff --check` 通过。

核心回归命令：

```text
mvn -q "-Dtest=QingheArchitectureBoundaryTest,QingheIsolatedContextTest,MemberCenterMockServerTest,OpenApiContractStructureTest,PosBusinessContractSkeletonTest,PosContractClientTest,ReconciliationContractFixtureTest,AuthenticationBoundaryTest,HttpMemberCenterClientTest,MemberSessionServiceTest,PosRequestAuthenticatorTest,RedisPlatformSessionCacheTest,QingheSchemaMigrationContractTest,BusinessIdGeneratorTest,MoneyFenTest,TraceIdFilterTest,TraceIdTest,StoreEligibilityAndCredentialServiceTest,StoreImportServiceTest,QingheApiControllerTest,VoucherOrderTransactionServiceTest" test
```

真实 MySQL 集成测试通过环境变量注入本机连接信息执行，不在命令、源码或文档中记录凭据。

## 已确认但未冒充完成

- 会员中心是仓库内 Mock 契约，不是已对接的真实厂商系统；
- 管理端 Bearer Token 是本地/集成环境适配器，不是企业统一身份平台；
- POS 密钥解析目前提供环境变量适配器，生产 Vault/KMS 需要部署环境实现；
- Redis 缓存已通过隔离测试验证，但没有生产部署或真实流量证据；
- 当前证明停用门店无法通过 POS 认证入口。验券、核销、原结果查询和补贴候选仍由 WP-06 实现；
- 没有真实甲方、真实门店、生产发布或性能数字。

## 下一安全动作

1. 基于 PRD、接口契约与技术方案复核 WP-03 的状态机和发布快照；
2. 若现有 V001 表不足，以 V003 前向迁移补充审批事实和不可变快照，不改写 V001/V002；
3. 先实现活动发布与库存调整的领域门禁，再接管理 API 和 Redis 库存初始化；
4. WP-03 Gate 通过前不开始依赖它的 WP-04。
